import { SiteLayout,Section,Eyebrow } from '@/components/SiteLayout';
export function InformationPage({title,intro,sections}:{title:string;intro:string;sections:Array<[string,string]>}){return <SiteLayout><Section className="max-w-4xl"><Eyebrow>BraverTogether</Eyebrow><h1 className="mt-5 text-4xl font-bold sm:text-5xl">{title}</h1><p className="mt-5 text-lg text-muted-foreground">{intro}</p><div className="mt-10 space-y-8">{sections.map(([heading,body])=><section key={heading}><h2 className="text-2xl font-bold">{heading}</h2><p className="mt-3 whitespace-pre-line leading-relaxed text-muted-foreground">{body}</p></section>)}</div></Section></SiteLayout>}
