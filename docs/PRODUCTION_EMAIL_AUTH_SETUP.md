# BraverTogether production auth email setup

Production domain: `https://bravertogether.site`

This project uses Supabase Auth for account email and Resend as the SMTP delivery provider. Keep all SMTP/API credentials in Supabase or another server-side secret store; never expose them through a `VITE_` environment variable or commit them to the repository.

## 1. Resend sending domain

Use a dedicated authentication subdomain:

```text
auth.bravertogether.site
```

Verify that exact subdomain in Resend. Add the SPF/MX and DKIM DNS records generated by Resend to the authoritative DNS provider for `bravertogether.site`. Add DMARC after SPF and DKIM are passing.

Recommended sender:

```text
BraverTogether <no-reply@auth.bravertogether.site>
```

The sender address does not need a separate mailbox in Resend, but replies should not be encouraged from authentication messages.

Disable Resend open tracking and click/link tracking for this authentication domain. Authentication emails should contain no marketing content.

Create a Resend API key named `BraverTogether Supabase Auth` with **Sending access**, restricted to `auth.bravertogether.site`. Copy the key once and store it directly in the Supabase SMTP password field. Do not add it to the frontend or repository.

## 2. Supabase custom SMTP

Configure **Authentication → Emails → SMTP Settings**:

```text
Enable custom SMTP: on
Sender email: no-reply@auth.bravertogether.site
Sender name: BraverTogether
Host: smtp.resend.com
Port: 465
Username: resend
Password: <Resend sending-only API key>
```

Keep email authentication enabled and keep automatic email confirmation disabled. The member signup flow verifies the email OTP itself before the user sets a password.

## 3. Supabase URL configuration

Set the hosted Auth Site URL to:

```text
https://bravertogether.site
```

Allow these redirect URLs at minimum:

```text
https://bravertogether.site/auth
https://bravertogether.site/auth?mode=signup
https://bravertogether.site/auth?mode=signin
https://bravertogether.site/auth?mode=signin&recovery=1
```

Keep the development URLs that are still actively used by the team, but do not use a preview/dev URL as the Site URL in production.

## 4. Email OTP configuration

The signup implementation uses `signInWithOtp` and `verifyOtp`, so the **Magic Link** template must render `{{ .Token }}` rather than only `{{ .ConfirmationURL }}`.

Subject:

```text
{{ .Token }} is your BraverTogether verification code
```

HTML:

```html
<!doctype html>
<html>
  <body style="margin:0;background:#f6f7f9;font-family:Arial,Helvetica,sans-serif;color:#12223a;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="padding:32px 16px;background:#f6f7f9;">
      <tr>
        <td align="center">
          <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:520px;background:#ffffff;border:1px solid #e4e7ec;border-radius:16px;padding:32px;">
            <tr><td style="font-size:22px;font-weight:700;padding-bottom:12px;">Verify your email</td></tr>
            <tr><td style="font-size:14px;line-height:1.6;padding-bottom:20px;">Enter this six-digit code to continue creating your BraverTogether account.</td></tr>
            <tr><td align="center" style="font-size:32px;font-weight:700;letter-spacing:8px;padding:16px 8px;background:#f6f7f9;border-radius:12px;">{{ .Token }}</td></tr>
            <tr><td style="font-size:12px;line-height:1.6;color:#667085;padding-top:20px;">If you did not request this code, you can ignore this email. Do not share the code with anyone.</td></tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
```

Keep the OTP length at six digits because the current UI is six slots. Recommended expiry: 10 minutes (600 seconds). Keep the resend interval at 60 seconds to match the UI cooldown.

## 5. Password recovery template

Use the Supabase-generated confirmation URL for recovery. It preserves the secure recovery token and the application-provided redirect URL.

Subject:

```text
Reset your BraverTogether password
```

HTML:

```html
<!doctype html>
<html>
  <body style="margin:0;background:#f6f7f9;font-family:Arial,Helvetica,sans-serif;color:#12223a;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="padding:32px 16px;background:#f6f7f9;">
      <tr>
        <td align="center">
          <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:520px;background:#ffffff;border:1px solid #e4e7ec;border-radius:16px;padding:32px;">
            <tr><td style="font-size:22px;font-weight:700;padding-bottom:12px;">Reset your password</td></tr>
            <tr><td style="font-size:14px;line-height:1.6;padding-bottom:20px;">A password reset was requested for your BraverTogether account.</td></tr>
            <tr><td align="center"><a href="{{ .ConfirmationURL }}" style="display:inline-block;background:#15334d;color:#ffffff;text-decoration:none;font-weight:700;padding:12px 22px;border-radius:999px;">Reset password</a></td></tr>
            <tr><td style="font-size:12px;line-height:1.6;color:#667085;padding-top:20px;">If you did not request a reset, ignore this email. This link is single-use.</td></tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
```

## 6. Email-change and security notifications

Keep **Secure email change** enabled. If account email changes are exposed in the product, configure the Email Change template and require verification before the new address becomes authoritative.

Enable Supabase security notification emails for at least:

- password changed
- email changed
- identity linked
- identity unlinked
- MFA factor enrolled/unenrolled if MFA is later exposed

Authentication/security notification emails should use the same `auth.bravertogether.site` sender and remain short and transactional.

## 7. Rate limits and abuse controls

After custom SMTP is enabled, review **Authentication → Rate Limits**. Do not raise limits until the normal flows have been tested. Signup OTP, recovery, and resend endpoints should remain rate-limited.

Before public launch, configure Supabase Auth CAPTCHA protection for public signup and password recovery if bot traffic is expected.

## 8. Required end-to-end email tests

Run these using real inboxes from at least Gmail and Outlook:

1. New member signup sends a six-digit OTP.
2. Correct OTP verifies; wrong/expired OTP is rejected.
3. Resend is blocked during the 60-second cooldown, then sends a fresh code.
4. Setting the initial password completes the account and the session works after refresh.
5. Existing account cannot accidentally restart signup as a new account.
6. Forgot-password request returns a neutral success message whether or not the account exists.
7. Recovery email arrives and its button lands only on `https://bravertogether.site`.
8. Recovery token can be used once; a reused/expired link fails safely.
9. New password works; old password no longer works.
10. Password-changed security notification arrives.
11. If email change is enabled in the UI, both the confirmation and email-changed security notification are tested.
12. Google OAuth remains disabled until its provider credentials and exact production callback URLs are configured.
13. No auth email contains a Resend tracking redirect.
14. SPF and DKIM pass; DMARC alignment is checked after DMARC is published.
15. Supabase Auth logs and Resend delivery logs show no unexpected rejects, bounces or rate-limit errors.
